'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = group;
function group() {
  return {
    name: 'group',

    parse: function parse(text, info) {
      var lines = text.trim().split('\n');
      var slug = lines[0].trim().toLowerCase();
      var description = lines.splice(1).join('\n').trim();
      if (description) {
        info.groupDescriptions = info.groupDescriptions || {};
        info.groupDescriptions[slug] = description;
      }
      return [slug];
    },
    default: function _default() {
      return ['undefined'];
    },


    multiple: false
  };
}